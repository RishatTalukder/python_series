[project]
name = "webite-status-checker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Md. Rishat Talukder", email = "120198784+RishatTalukder@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
webite-status-checker = "webite_status_checker:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
