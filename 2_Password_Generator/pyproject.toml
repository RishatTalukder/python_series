[project]
name = "password-generator"
version = "0.1.0"
description = "A CLI password generator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
password-generator = "main:main"
